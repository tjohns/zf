<!-- EN-Revision: 13565 -->
<sect1 id="zend.exception.using">
    <title>Utiliser les exceptions</title>

    <para>Toutes les exceptions levées par les classes de Zend Framework peuvent être une exception qui dérive de la
    classe de base <code>Zend_Exception</code>.</para>

    <example id="zend.exception.using.example">
        <title>Récupération d'une exception</title>
        <para>Le code suivant montre comment attraper une exception levée par une classe de Zend Framework :</para>

        <programlisting role="php"><![CDATA[
try {
    Zend_Loader::loadClass('classnonexistante');
} catch (Zend_Exception $e) {
    // Appeler Zend_Loader::loadClass() sur une classe non-existante
    //entrainera la levée d'une exception dans Zend_Loader
    echo "Récupère exception: " . get_class($e) . "\n";
    echo "Message: " . $e->getMessage() . "\n";
    // puis tout le code nécessaire pour récupérer l'erreur
}
]]></programlisting>
    </example>

    <para>La documentation pour chaque composant de Zend Framework et de ses classes contient les informations plus
    spécifiques sur les méthodes qui lèvent des exceptions, les circonstances de lancement de ces exceptions et quelles
    classes d'exception dérivent de <code>Zend_Exception</code>.</para>
</sect1>