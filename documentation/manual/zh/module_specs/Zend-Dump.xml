<sect1 id="zend.dump">
    <title>Zend::dump()</title>

    <para>
    <code>Zend::dump()</code> 打印和返回某个表达式或变量的信息。它是将var_dump()封装而成的。它默认带上了(X)HTML的pre标签，使输出更美观。 
    </para>

    <programlisting role="php"><![CDATA[<?php

Zend::dump($var, $label=null, $echo=true)

?>]]></programlisting>

    <para>
    $var 参数指定要输出信息的变量或表达式。
    </para>

    <para>
   $label标签参数是用来加在输出信息之前的一段文本。
    </para>

    <para>dump()总会返回信息，但不一定会输出，这取决于$echo参数，指定是否要将信息输出到屏幕。
    </para>

	<para>
	(这非常有用，例如你一次要查看多个变量的信息，你可以为不同变量设置不同label，如“user”，“password”等等，这样不会弄乱--Haohappy注)
	</para>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->