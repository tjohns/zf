<sect1 id="zend.isreadable">
    <title>Zend::isReadable()</title>

    <para>检查某个文件是否可读，可读则返回true，否则返回false。</para>

    <programlisting role="php"><![CDATA[<?php

Zend::isReadable($filename)

?>]]></programlisting>

    <para><code>$filename</code>指定要检查的文件，可以包含路径。这个方法是将PHP函数<ulink url="http://php.net/is_readable"><code>is_readable()</code></ulink>封装而成的，is_readable()不会自动查找include_path下的文件，而Zend::isReadable()可以。
    </para>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->