<sect1 id="zend.mime.introduction">
    <title>Introduction</title>
    <para>
        <code>Zend_Mime</code> is a support class for handling multipart MIME messages. It is used by <link
        linkend="zend.mail"><code>Zend_Mail</code></link>, and may be used by applications requiring
        MIME support.
    </para>
    <para>
        <code>Zend_Mime</code> provides a simple set of methods to work with MIME:
        <itemizedlist>
            <listitem>
                <code>boundary()</code>: Returns the MIME boundary string.
            </listitem>
            <listitem>
                <code>boundaryLine()</code>: Returns the complete MIME boundary line.
            </listitem>
            <listitem>
                <code>encodeBase64()</code>: Encodes a string into base64 encoding.
            </listitem>
            <listitem>
                <code>encodeQuotedPrintable()</code>: Encodes a string with the quoted-printable mechanism.
            </listitem>
            <listitem>
                <code>isPrintable()</code>: Returns TRUE if the given string contains no unprintable characters,
                FALSE otherwise.
            </listitem>
            <listitem>
                <code>mimeEnd()</code>: Returns the complete MIME end boundary line.
            </listitem>
        </itemizedlist>
    </para>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->