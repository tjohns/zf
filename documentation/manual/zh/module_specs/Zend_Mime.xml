<sect1 id="zend.mime.introduction">
    <title>简介</title>
    <para>
        <code>Zend_Mime</code> 是一个用来处理含有多个部分的 MIME 消息的辅助类. 它未 <link
        linkend="zend.mail"><code>Zend_Mail</code></link> 提供支持, 并且可以被其他需要 MIME 支持的应用程序使用.
    </para>
    <para>
        <code>Zend_Mime</code> 提供了一套简单的方法以便使用 MIME:
        <itemizedlist>
            <listitem>
                <code>boundary()</code>: 返回 MIME 分界字符串.
            </listitem>
            <listitem>
                <code>boundaryLine()</code>: 返回完整的 MIME 分界行.
            </listitem>
            <listitem>
                <code>encodeBase64()</code>: 将一个字符串进行 base64 编码.
            </listitem>
            <listitem>
                <code>encodeQuotedPrintable()</code>: 使用 quoted-printable 机制对字符串进行编码.
            </listitem>
            <listitem>
                <code>isPrintable()</code>: 如果给定的字符串不含有不可显示(译者：原文是 unprintable ) 的字符则返回 TRUE, 否则返回 FALSE. 
            </listitem>
            <listitem>
                <code>mimeEnd()</code>: 返回完整的 MIME 结束标志行.
            </listitem>
        </itemizedlist>
    </para>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->