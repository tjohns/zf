<sect1 id="zend.measure.writing">

    <title>Zend Framework での計測値の扱い</title>

    <para>
        <code>Zend_Measure</code> は、さまざまな計測単位をサポートしています。
        それぞれの計測値の型が、固有の物理的あるいは数値的な性質を扱っています。
    </para>

    <para>
        計測単位は、すべて
        <code>Measurement_Type::NAME_OF_UNIT</code>
        のような共通の表記で表します。個々の単位は、
        他の単位との変換式や表示単位で構成されます。
        <example>
            <title><code>メートル</code> の値</title>
            <para>
                <code>メートル</code> は長さをはかるために使用するものです。
                したがって、この型を表す定数は <code>Length</code> クラスにあります。
                この単位を使用するには、<code>Length::METER</code>
                という表記を使用しなければなりません。単位として表示される内容は
                <code>m</code> です。
            </para>
<programlisting role="php"><![CDATA[<?php
require_once 'Zend.php';
Zend::loadClass('Zend_Measure');
// Length クラスを読み込みます
Zend::loadClass('Zend_Measure_Length');

echo Zend_Measure::LENGTH;           // 出力は 'Zend_Measure::LENGTH' となります
echo Zend_Measure_Length::STANDARD;  // 出力は 'Length::METER' となります
echo Zend_Measure_Length::KILOMETER; // 出力は 'Length::KILOMETER' となります

$locale = new Zend_Locale('en');
$unit = new Zend_Measure(100,Zend_Measure_Length::METER,$locale);
echo $unit;
// 出力は '100 m' となります
?>]]></programlisting>
        </example>
    </para>

    <para>
        詳細な一覧は、
        <link linkend="zend.measure.types"><code>計測値の型</code>
        </link>
        の章にあります。
    </para>

</sect1>
<!--
vim:se ts=4 sw=4 et:
-->
