<!-- EN-Revision: 13947 -->
<sect1 id="zend.version.reading">

    <title>Zend Framework のバージョンの取得</title>

    <para>
        <code>Zend_Version</code> が提供するクラス定数
        <code>Zend_Version::VERSION</code> には、
        インストールされている Zend Framework のバージョン番号を表す文字列が含まれます。
        <code>Zend_Version::VERSION</code> は、たとえば "1.7.4" のようになります。
    </para>

    <para>
        静的メソッド <code>Zend_Version::compareVersion($version)</code>
        は、PHP の関数
        <ulink url="http://php.net/version_compare"><code>version_compare()</code></ulink>
        に基づいたものです。このメソッドは、指定したバージョンが
        Zend Framework のバージョンより古い場合に -1、
        同じ場合に 0、そして指定したバージョンのほうが
        Zend Framework のバージョンより新しい場合に +1 を返します。
    </para>

    <example id="zend.version.reading.example">
        <title><code>compareVersion()</code> メソッドの例</title>
        <programlisting role="php"><![CDATA[
// -1、0 あるいは 1 を返します
$cmp = Zend_Version::compareVersion('2.0.0');
]]>
        </programlisting>
    </example>

</sect1>
<!--
vim:se ts=4 sw=4 et:
-->
