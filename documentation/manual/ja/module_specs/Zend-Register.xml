<sect1 id="zend.register">
    <title>Zend::register()</title>

    <para>
    値をレジストリに登録します。
    </para>

    <programlisting role="php"><![CDATA[<?php

Zend::register($index, $value)

?>]]></programlisting>

    <para>
    レジストリとは、
    値のシングルトンインスタンスをアプリケーション空間に提供する仕組みのことです。
    値をレジストリに保存すると、
    必要なときにいつでもそれを取り出して使用できます。
    その際、常に同一のインスタンスが返されます。
    </para>

    <para>
    <code>$index</code> には、保存する値の名前を任意の文字列で指定します。
    <code>Zend::registry()</code> で値を取得する際に、この名前を使用します。
    </para>

    <para>
    このレジストリは、ArrayObject となります。
    </para>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->