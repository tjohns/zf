<!-- EN-Revision: 12661 -->
<sect1 id="zend.file.transfer.validators">

    <title>Prüfungen für Zend_File_Transfer</title>

    <para>
        <code>Zend_File_Transfer</code> wird mit verschiedenen Datei-relevanten Prüfungen ausgeliefert
        welche verwendet werden sollten um die Sicherheit zu erhöhen und mögliche Attacken zu verhindern.
        Es ist zu beachten das die Prüfungen nur so gut sind wie der der Sie verwendet. Alle Prüfungen
        die von <code>Zend_File_Transfer</code> angeboten werden können in der <code>Zend_Validator</code>
        Komponente gefunden werden und heißen <code>Zend_Validate_File_*</code>. Die folgenden Prüfungen
        sind aktuell vorhanden:
    </para>

    <itemizedlist>
        <listitem>
            <para>
                <code>Count</code>: Diese Prüfung checkt die Anzahl der Dateien. Er bietet ein Minimum und ein
                Maximum an und wirft einen Fehler wenn einer der zwei Werte überschritten werden.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Crc32</code>: Diese Prüfung checkt den Crc32 Hashwert vom Inhalt einer Datei. Sie basiert
                auf der <code>Hash</code> Prüfung und erlaubt eine einfachere Verwendung indem nur Crc32
                unterstützt wird.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>ExcludeExtension</code>: Diese Prüfung checkt die Dateierweiterung. Sie wirft einen Fehler
                wenn die angegebene Datei eine definierte Erweiterung hat. Auf diesem Weg können definierte
                Erweiterungen von der Prüfung ausgenommen werden.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>ExcludeMimeType</code>: Diese Prüfung kann den Mimetyp von Dateien überprüfen. Sie ist auch
                fähig Arten von Mimetypen zu prüfen und wirft einen Fehler wenn der MimeTyp einer angegebenen
                Datei passt.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Exists</code>: Diese Prüfung checkt ob Dateien existieren. Sie wirft einen Fehler wenn
                die angegebene Datei nicht existiert.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Extension</code>: Diese Prüfung checkt die Dateierweiterung. Sie wirft einen Fehler
                wenn eine angegebene Datei eine nicht definierte Erweiterung hat.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>FilesSize</code>: Diese Prüfung checkt die gesamte Größe aller geprüften Dateien.
                Sie merkt sich intern die Größe aller geprüften Dateien und wirft einen Fehler wenn die
                Summe aller Dateien die definierte Größe überschreitet. Sie bietet auch eine Mindest- und
                Maximalgröße an.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>ImageSize</code>: Diese Prüfung checkt die Größe von Bildern. Sie prüft die Breite
                und Höhe und bietet sowohl eine Mindest- als auch eine Maximalgröße.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>IsCompressed</code>: Diese Prüfung checkt ob die Datei komprimiert ist. Sie basiert
                auf der <code>MimeType</code> Prüfung und validiert Komprimierungsarchiven wie Zip
                oder Arc. Sie kann auch auf spezielle Archive begrenzt werden.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>IsImage</code>: Diese Prüfung checkt ob die Datei ein Bild ist. Sie basiert auf der
                <code>MimeType</code> Prüfung und validiert Bildedateien wie Jpg oder Gif. Sie kann auch
                auf spezielle Bildtypen begrenzt werden.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Hash</code>: Diese Prüfung check den Hashwert des Inhalts einer Datei. Sie unterstützt
                mehrere Algorithmen.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Md5</code>: Diese Prüfung checkt den Md5 Hashwert vom Inhalt einer Datei. Sie basiert
                auf der <code>Hash</code> Prüfung und erlaubt eine einfachere Verwendung indem nur Md5
                unterstützt wird.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>MimeType</code>: Diese Prüfung kann den Mimetyp von Dateien überprüfen. Sie ist auch
                fähig Arten von Mimetypen zu prüfen und wirft einen Fehler wenn der MimeTyp einer angegebenen
                Datei nicht passt.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>NotExists</code>: Diese Prüfung checkt ob Dateien existieren. Sie wirft einen Fehler
                wenn eine angegebene Datei existiert.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Sha1</code>: Diese Prüfung checkt den Sha1 Hashwert vom Inhalt einer Datei. Sie basiert
                auf der <code>Hash</code> Prüfung und erlaubt eine einfachere Verwendung indem nur Sha1
                unterstützt wird.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Size</code>: Diese Prüfung ist fähig Dateien auf Ihre Dateigröße zu prüfen. Sie
                bietet eine Mindest- und eine Maximalgröße an und wirft einen Fehler wenn eine der
                zwei überschritten wird.
            </para>
        </listitem>

        <listitem>
            <para>
                <code>Upload</code>: Diese Prüfung ist eine interne, die prüft ob ein Upload Probleme
                produziert hat oder nicht. Dieser darf nicht gesetzt werden, da er automatisch durch
                <code>Zend_File_Transfer</code> selbst gesetzt wird. Deshalb kann diese Prüfung
                vergessen werden. Man sollte nur wissen das Sie existiert.
            </para>
        </listitem>
    </itemizedlist>

    <sect2 id="zend.file.transfer.validators.usage">
        <title>Prüfungen mit <code>Zend_File_Transfer</code> verwenden</title>

        <para>
            Die Verwendung von Prüfungen ist sehr einfach. Es gibt verschiedene Methoden für das
            Hinzufügen und Manipulieren von Prüfungen.
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>isValid($files = null)</code>: Prüft die angegebenen Dateien indem alle gesetzten
                    Prüfer verwendet werden. <code>$files</code> kann entweder ein richtiger Dateiname, oder
                    der Name des Elements, oder der Name der temporären Datei sein.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>addValidator($validator, $breakChainOnFailure, $options = null, $files = null)</code>: Fügt die
                    angegebene Prüfung zu den Prüfungsstapel hinzu (optional nur zu den Datei(en) die
                    spezifiziert wurden). <code>$validator</code> kann entweder eine aktuelle
                    Prüfinstanz sein, oder ein Kurzname der den Prüfungstyp spezifiziert (z.B. 'Count').
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>addValidators(array $validators, $files = null)</code>: Fügt die angegebenen
                    Prüfungen zum Prüfungsstapel hinzu. Jeder Eintrag kann entweder ein Prüfungstyp/-options
                    Paar sein, oder ein Array mit dem Schlüssel 'validator' das die Prüfung spezifiziert
                    (alle anderen Optionen werden als Optionen für die Instanzierung der Prüfung angenommen).
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>setValidators(array $validators, $files = null)</code>: Überschreibt alle bestehenden
                    Prüfungen mit den spezifizierten Prüfungen. Die Prüfungen sollten der Syntax folgen die
                    für <code>addValidators()</code> definiert ist.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>hasValidator($name)</code>: Zeigt ob eine Prüfung registriert wurde.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>getValidator($name)</code>: Gibt eine vorher registrierte Prüfung zurück.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>getValidators($files = null)</code>: Gibt registrierte Prüfungen zurück; wenn
                    <code>$files</code> übergeben wurde, werden die Prüfungen für die betreffende Datei
                    oder das Set an Dateien zurückgegeben.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>removeValidator($name)</code>: Entfernt eine vorher registrierte Prüfung.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>clearValidators()</code>: Löscht alle registrierten Prüfungen.
                </para>
            </listitem>
        </itemizedlist>

        <example id="zend.file.transfer.validators.usage.example">
            <title>Prüfungen zu einem Dateitransfer hinzufügen</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Setzt eine Dateigröße von 20000 Bytes
$upload->addValidator('Size', false, 20000);

// Setzt eine Dateigröße von mindestens 20 Bytes und maximal 20000 Bytes
$upload->addValidator('Size', false, array('min' => 20, 'max' => 20000));

// Setzt eine Dateigröße von mindestens 20 Bytes und Maximal
// 20000 Bytes und eine Dateianzahl in einem Schritt
$upload->setValidators(array(
    'Size'  => array('min' => 20, 'max' => 20000),
    'Count' => array('min' => 1, 'max' => 3),
));
]]>
            </programlisting>
        </example>

        <example id="zend.file.transfer.validators.usage.exampletwo">
            <title>Prüfungen auf eine einzelne Datei limitieren</title>

            <para>
                <code>addValidator()</code>, <code>addValidators()</code>, und <code>setValidators()</code>
                akzeptieren ein endendes Argument <code>$files</code>. Dieses Argument kann verwendet werden
                um eine Datei oder ein Array von Dateien zu spezifizieren auf dem die angegebene Prüfung
                gesetzt werden soll.
            </para>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Setzt die Dateigröße auf 20000 Bytes und limitiert Sie nur auf 'file2'
$upload->addValidator('Size', false, 20000, 'file2');
]]>
            </programlisting>
        </example>

        <para>
            Generell sollte einfach die <code>addValidators()</code> Methode verwendet werden, welche
            mehrmals aufgerufen werden kann.
        </para>

        <example id="zend.file.transfer.validators.usage.examplemultiple">
            <title>Mehrere Prüfungen hinzufügen</title>

            <para>
                Oft ist es einfacher <code>addValidator()</code> mehrere Male aufzurufen. Ein Aufruf für
                jede Prüfung. Das erhöht auch die Lesbarkeit und macht den Code wartbarer. Da alle
                Methoden das Fluent-Interface implementieren können Aufrufe einfach wie anbei gezeigt
                gekoppelt werden:
            </para>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Setzt die Dateigröße auf 20000 Bytes
$upload->addValidator('Size', false, 20000)
       ->addValidator('Count', false, 2)
       ->addValidator('Filessize', false, 25000);
]]>
            </programlisting>

        </example>

        <note>
            <para>
                Es ist zu beachten das, auch wenn das mehrfache Setzen der gleichen Prüfung erlaubt
                ist, das zu Problemen führen kann wenn verschiedene Optionen für die gleiche
                Prüfung verwendet werden.
            </para>
        </note>

        <para>
            Und letztendlich können Dateien einfach geprüft werden indem <code>isValid()</code> verwendet wird.
        </para>

        <example id="zend.file.transfer.validators.usage.exampleisvalid">
            <title>Prüfen der Dateien</title>

            <para>
                <code>isValid()</code> akzeptiert die hoch- oder heruntergeladenen Dateinamen, den temporären
                Dateinamen und natürlich den Namen des Formularelements. Wenn kein Parameter oder null angegeben
                wird, werden alle gefundenen Dateien geprüft.
            </para>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Setzt die Dateigröße auf 20000 Bytes
$upload->addValidator('Size', false, 20000)
       ->addValidator('Count', false, 2)
       ->addValidator('Filessize', false, 25000);

if ($upload->isValid()) {
    print "Prüfung fehlgeschlagen";
}
]]>
            </programlisting>

        </example>

        <note>
            <para>
                Es ist zu beachten das <code>isValid()</code> automatisch aufgerufen wird, wenn die Dateien
                empfangen werden und es zuvor noch nicht aufgerufen wurde.
            </para>
        </note>

        <para>
            Wenn Prüfungen fehlgeschlagen sind, ist es normalerweise eine gute Idee Informationen über die
            gefundenen Probleme zu erhalten. Hierfür können die Methoden <code>getMessages()</code>,
            welche alle Prüfmeldungen als Array zurückgibt, <code>getErrors()</code> welche alle Fehlercodes
            zurückgibt, und <code>hasErrors()</code> welche true zurückgibt sobald ein Prüffehler gefunden wurde,
            verwendet werden.
        </para>

    </sect2>

    <sect2 id="zend.file.transfer.validators.count">
        <title><emphasis role="strong">Count</emphasis> Prüfung</title>

        <para>
            Die <code>Count</code> Prüfung checkt die Anzahl der Dateien die angegeben wurde. Sie unterstützt
            die folgenden Schlüssel für Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>min</code>: Setzt die minimale Anzahl der Dateien die übertragen wird.
                </para>

                <note>
                    <para>
                        Achtung: Wenn diese Option verwendet wird, muß die minimale Anzahl an Dateien
                        übergeben werden wenn die Prüfung das erste Mal aufgerufen wird; sonst wird ein
                        Fehler zurückgegeben.
                    </para>
                </note>

                <para>
                    Mit dieser Option kann die Mindestanzahl an Dateien definiert werden die man Empfangen sollte.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>max</code>: Setzt die maximale Anzahl an Dateien die übertragen wird.
                </para>

                <para>
                    Mit dieser Option kann die Anzahl der Dateien limitiert werden die man akzeptiert,
                    aber genauso eine mögliche Attacke erkennen wenn mehr Dateien übertragen werden als
                    im Formular definiert wurden.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Wenn diese Prüfung mit einem String oder Integer initiiert wird, wird Sie als
            <code>max</code> verwendet. Es können aber auch die Methoden <code>setMin()</code> und
            <code>setMax()</code> verwendet werden um beide Optionen im Nachhinein zu setzen und
            <code>getMin()</code> und <code>getMax()</code> um die aktuell gesetzten Werte zu erhalten.
        </para>

        <example id="zend.file.transfer.validators.count.example">
            <title>Die Count Prüfung verwenden</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Die Anzahl der Dateien auf maximal 2 limitieren
$upload->addValidator('Count', false, 2);

// Die Anzahl der Datein auf Maximal 5 limitieren, und mindestens
// 1 Datei als Rückgabe erwarten
$upload->addValidator('Count', false, array('min' =>1, 'max' => 5));
]]>
            </programlisting>

        </example>

        <note>
            <para>
                Beachte das diese Prüfung die Anzahl der geprüften Dateien intern speichert. Die Datei
                welche das Maximum überschrietet wird als Fehler zurückgegeben.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.crc32">
        <title><emphasis role="strong">Crc32</emphasis> Prüfung</title>

        <para>
            Die <code>Crc32</code> Prüfung checkt den Inhalt einer übertragenen Datei durch hashen. Diese
            Prüfung verwendet die Hash Erweiterung von PHP mit dem Crc32 Algorithmus. Sie unterstützt die
            folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>hash</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Dieser Wert wird als Hash verwendet gegen den geprüft wird.
                </para>

                <para>
                    Man kann mehrere Hashes setzen indem unterschiedliche Schlüssel angegeben werden.
                    Jeder von Ihnen wird geprüft und nur wenn alle fehlschlagen wird die Prüfung
                    selbst fehlschlagen.
                </para>
            </listitem>
        </itemizedlist>

        <example id="zend.file.transfer.validators.crc32.example">
            <title>Verwenden der Crc32 Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Prüft ob der Inhalt der hochgeladenen Datei den angegebenen Hash hat
$upload->addValidator('Hash', false, '3b3652f');

// Begrenzt diese Prüfung auf zwei unterschiedliche Hashes
$upload->addValidator('Crc32', false, array('3b3652f', 'e612b69'));
]]>
            </programlisting>
        </example>
    </sect2>

    <sect2 id="zend.file.transfer.validators.excludeextension">
        <title><emphasis role="strong">ExcludeExtension</emphasis> Prüfung</title>

        <para>
            Die <code>ExcludeExtension</code> Prüfung checkt die Dateierweiterung von Dateien dir angegeben
            werden. Sie unterstützt die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Dieser Wert wird als Dateierweiterung verwendet um zu prüfen ob die
                    angegebene Datei diese Dateierweiterung nicht verwendet.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>case</code>: Definiert of die Prüfung auf die Klein/Großschreibung achten soll.
                    Standardmäßig ist die Klein/Großschreibung egal. Es ist zu beachten das dieser Schlüssel
                    für alle verwendeten Erweiterungen verwendet wird.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Dieser Prüfer akzeptiert mehrere Erweiterungen entweder als komma-getrennten String, oder als
            ein Array. Man kann auch die <code>setExtension()</code>, <code>addExtension()</code> und
            <code>getExtension()</code> Methoden verwenden um Erweiterungen zu setzen und zu erhalten.
        </para>

        <para>
            In einigen Fällen ist es nützlich auch auf Klein/Großschreibung zu testen. Hierfür erlaubt
            der Constructor einen zweiten Parameter <code>$case</code> der, wenn er auf true gesetzt
            wird, die Erweiterungen abhängig von der Klein/Großschreibung prüft.
        </para>

        <example id="zend.file.transfer.validators.excludeextension.example">
            <title>Die ExcludeExtension Prüfung verwenden</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Dateien mit der Erweiterung php oder exe nicht erlauben
$upload->addValidator('ExcludeExtension', false, 'php,exe');

// Dateien mit der Erweiterung php oder exe nicht erlauben aber die Array Schreibweise verwenden
$upload->addValidator('ExcludeExtension', false, array('php', 'exe'));

// Prüft abhängig von der Groß-/Kleinschreibung
$upload->addValidator('ExcludeExtension', false, array('php', 'exe', 'case' => true));
]]>
            </programlisting>

        </example>
        <note>
            <para>
                Es ist zu beachten das diese Prüfung nur die Dateierweiterung prüft. Sie prüft nicht
                den MIME Typ der aktuellen Datei.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.excludemimetype">
        <title><emphasis role="strong">ExcludeMimeType</emphasis> Prüfung</title>

        <para>
            Die <code>MimeType</code> Prüfung checkt den Mimetyp von übertragenen Dateien. Sie unterstützt
            die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array angeben.
                    Setzt die Art des Mimetyps der geprüft werden soll.
                </para>

                <para>
                    Mit dieser Option kann der Mimetyp von Dateien definiert werden der akzeptiert werden soll.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Diese Prüfung akzeptiert viele Mimetypen entweder als Komma-getrennter String, oder als
            Array. Man kan auch die Methoden <code>setMimeType()</code>, <code>addMimeType()</code>,
            und  <code>getMimeType()</code> verwenden um Mimetypen zu setzen und zu erhalten.
        </para>

        <example id="zend.file.transfer.validators.excludemimetype.example">
            <title>Verwendung der ExcludeMimeType Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Verweigert den Mimetyp gif für alle gegebenen Dateien
$upload->addValidator('ExcludeMimeType', 'image/gif');

// Verweigert den Mimetyp gif und jpeg für alle gegebenen Dateien
$upload->setValidator('ExcludeMimeType', array('image/gif',
                                               'image/jpeg');

// Verweigert die Mimetyp Gruppe image für alle Dateien
$upload->setValidator('ExcludeMimeType', 'image');
]]>
            </programlisting>
        </example>

        <para>
            Das obige Beispiel zeigt das es auch möglich ist den verweigerten Mimetyp auf eine Gruppe von
            Mimetypen zu begrenzen. Um alle Bilder zu verweigern kann einfach 'image' als Mimetyp verwendet
            werden. Das kann für alle Gruppen wie 'image', 'audio', 'video', 'test', und so weiter
            gemacht werden.
        </para>

        <note>
            <para>
                Es ist zu beachten das bei Verwendung von Mimetyp-Gruppen alle Mitglieder dieser Gruppe
                verweigert werden, selbst wenn das nicht gewünscht ist. Wenn man 'image' verweigert,
                werden alle Typen von Bildern verweigert wie 'image/jpeg' oder 'image/vasa'. Wenn man
                nicht sicher ist ob man alle Typen verweigern will ist es besser nur
                definierte Mimetypen zu verweigern statt der kompletten Gruppe.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.exists">
        <title><emphasis role="strong">Exists</emphasis> Prüfung</title>

        <para>
            Die <code>Exists</code> Prüfung checkt ob Dateien die angegeben werden existieren. Sie unterstützt
            die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array angeben.
                    Checkt ob die Datei im gegebenen Verzeichnis existiert.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Diese Prüfung akzeptiert mehrere Verzeichnisse, entweder als Komma-getrennter String, oder als
            Array. Es können aber auch die Methoden <code>setDirectory()</code>, <code>addDirectory()</code>,
            und <code>getDirectory()</code> verwendet werden um die Verzeichnisse zu setzen und zu erhalten.
        </para>

        <example id="zend.file.transfer.validators.exists.example">
            <title>Die Exists Prüfung verwenden</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Das temporäre Verzeichnis für die Prüfung hinzufügen
$upload->addValidator('Exists', false, '\temp');

// Zwei Verzeichnisse hinzufügen inden die Array Schreibweise verwendet wird
$upload->addValidator('Exists', false, array('\home\images', '\home\uploads'));
]]>
            </programlisting>

        </example>
        <note>
            <para>
                Beachte das diese Prüfung checkt ob die Datei in allen gesetzten Verzeichnissen existiert.
                Die Prüfung schlägt fehl, wenn die Datei in irgendeinem der angegebenen Verzeichnisse nicht
                existiert.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.extension">
        <title><emphasis role="strong">Extension</emphasis> Prüfung</title>

        <para>
            Die <code>Extension</code> Prüfung checkt die Dateierweiterung der Dateien welche angegeben werden.
            Sie unterstützt die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array angeben.
                    Prüft ob die angegebene Datei diese Dateierweiterung verwendet.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>case</code>: Definiert of die Prüfung auf die Klein/Großschreibung achten soll.
                    Standardmäßig ist die Klein/Großschreibung egal. Es ist zu beachten das dieser Schlüssel
                    für alle verwendeten Erweiterungen verwendet wird.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Dieser Prüfer akzeptiert mehrere Erweiterungen entweder als komma-getrennten String, oder als
            ein Array. Man kann auch die <code>setExtension()</code>, <code>addExtension()</code> und
            <code>getExtension()</code> Methoden verwenden um Erweiterungen zu setzen und zu erhalten.
        </para>

        <para>
            In einigen Fällen ist es nützlich auch auf Klein/Großschreibung zu testen. Hierfür erlaubt
            der Constructor einen zweiten Parameter <code>$case</code> der, wenn er auf true gesetzt
            wird, die Erweiterungen abhängig von der Klein/Großschreibung prüft.
        </para>

        <example id="zend.file.transfer.validators.extension.example">
            <title>Verwendung der Extension Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Limitiert die Erweiterungen auf jpg und png Dateien
$upload->addValidator('Extension', false, 'jpg,png');

// Limitiert die Erweiterungen auf jpg und png Dateien, verwendet aber die Array Schreibweise
$upload->addValidator('Extension', false, array('jpg', 'png'));

// Abhängig von der Klein/Großschreibung prüfen
$upload->addValidator('Extension', false, array('mo', 'png', 'case' => true));
if (!$upload->isValid('C:\temp\myfile.MO')) {
    print 'Nicht gültig da MO statt mo verwendet wurde';
}
]]>
            </programlisting>

        </example>

        <note>
            <para>
                Es ist zu beachten das diese Prüfung nur die Dateierweiterung prüft. Sie prüft nicht
                den MIME Typ der aktuellen Datei.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.filessize">
        <title><emphasis role="strong">FilesSize</emphasis> Prüfung</title>

        <para>
            Die <code>FilesSize</code> Prüfung checkt die komplette Größe aller übertragenen Dateien.
            Sie unterstützt die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>min</code>: Setzt die minimale gemeinsame Dateigröße.
                </para>

                <para>
                    Mit dieser Option kann die mindeste gemeinsame Dateigröße der Dateien definiert werden
                    die man empfangen will.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>max</code>: Setzt die maximale gemeinsame Dateigröße.
                </para>

                <para>
                    Mit dieser Option kann die gemeinsame Dateigröße aller Dateien die übertragen wurden,
                    limitiert werden, aber nicht die Dateigröße von einzelnen Dateien.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>bytestring</code>: Definiert ob im Fehlerfall eine Benutzerfreundliche Nummer, oder
                    die reine Dateigröße zurückgegeben wird.
                </para>

                <para>
                    Mit dieser Option kann definiert werden ob der Benutzer '10864' oder '10MB' erhält. Der
                    Standardwert ist true was '10MB' zurückgibt.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Diese Prüfung kann auch nur mit einem String initiiert werden, der dann als
            <code>max</code> verwendet wird. Aber man kann auch die Methoden <code>setMin()</code> und
            <code>setMax()</code> verwenden um beide Optionen im Nachhinein zu setzen und
            <code>getMin()</code> sowie <code>getMax()</code> um die aktuell gesetzten Werte zu erhalten.
        </para>

        <para>
            Die Größe selbst wird auch in der SI Schreibweise akzeptiert wie Sie von den meisten
            Betriebsystemen angegeben wird. Statt 20000 Bytes kann man auch
            <emphasis role="strong">20kB</emphasis> angeben. Alle Einheiten werden mit dem Basiswert
            1024 konvertiert. Die folgenden Einheiten werden akzeptiert: <code>kB</code>, <code>MB</code>,
            <code>GB</code>, <code>TB</code>, <code>PB</code> und <code>EB</code>. Wie erwähnt muss man
            berücksichtigen das 1kB gleich 1024 Bytes entspricht.
        </para>

        <example id="zend.file.transfer.validators.filessize.example">
            <title>Verwenden der FilesSize Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Limitiert die Größe aller angegebenen Dateien auf 40000 Bytes
$upload->addValidator('FilesSize', false, 40000);

// Limitiert die Größe aller angegebenen Dateien auf maximal 4MB und minimal 10kB
$upload->addValidator('FilesSize', false, array('min' => '10kB', 'max' => '4MB'));

// Wie zuvor, gibt aber die reine Dateigröße statt einem benutzerfreundlichen String zurück
$upload->addValidator('FilesSize', false, array('min' => '10kB', 'max' => '4MB', 'bytestring' => false));
]]>
            </programlisting>

        </example>

        <note>
            <para>
                Es ist zu beachten das diese Prüfung die Dateigrößen der geprüften Dateien intern
                speichert. Die Datei welche die Größe überschreitet wird als Fehler zurückgegeben.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.imagesize">
        <title><emphasis role="strong">ImageSize</emphasis> Prüfung</title>

        <para>
            Die <code>ImageSize</code> Prüfung checkt die Größe von Bildern. Sie unterstützt die folgenden
            Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>minheight</code>: Setzt die mindeste Höhe des Bildes.
                </para>

                <para>
                    Mit dieser Option kann die Mindesthöhe des Bildes definiert werden das geprüft werden soll.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>maxheight</code>: Settzt die maximale Höhe des Bildes.
                </para>

                <para>
                    Mit dieser Option kann di Maximalhöhe des Bildes limitiert werden das geprüft werden soll.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>minwidth</code>: Setzt die mindeste Breite des Bildes.
                </para>

                <para>
                    Mit dieser Option kann die mindeste Breite des Bildes definiert werden das geprüft werden soll.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>maxwidth</code>: Setzt die maximale Breite des Bildes.
                </para>

                <para>
                    Mit dieser Option kann die maximale Breite des Bildes limitiert werden das geprüft werden soll.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Man kann auch die <code>setImageMin()</code> und <code>setImageMax()</code>
            Methoden verwenden um beide Minimal- und Maximalwerte im Nachhinein zu setzen und
            <code>getMin()</code> und <code>getMax()</code> um die aktuell gesetzten Werte zu erhalten.
        </para>

        <para>
            Der Bequemlichkeit halber gibt es auch die <code>setImageWidth</code> und <code>setImageHeight</code>
            Methoden welche die Mindest- und Maximalhöhe und -Breite setzen können. Natürlich sind auch
            passende <code>getImageWidth</code> und <code>getImageHeight</code> Methoden vorhanden.
        </para>

        <para>
            Um die Prüfung einer Dimension zu unterdrücken muß die betreffende Option einfach nicht
            gesetzt werden.
        </para>

        <example id="zend.file.transfer.validators.imagesize.example">
            <title>Verwendung der ImageSize Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Begrenzt die Größe eines Bildes auf eine Höhe von 100-200
// und eine Breite von 40-80 Pixel
$upload->addValidator('ImageSize', false,
                      array('minwidth' => 40,
                            'maxwidth' => 80,
                            'minheight' => 100,
                            'maxheight' => 200
                     );

// Setzt andere Dimensionen für das Bild
$upload->setImageWidth(array('minwidth' => 20, 'maxwidth' => 200));
]]>
            </programlisting>
        </example>
    </sect2>

    <sect2 id="zend.file.transfer.validators.iscompressed">
        <title><emphasis role="strong">IsCompressed</emphasis> Prüfung</title>

        <para>
            Die <code>IsCompressed</code> Prüfung checkt ob eine übertragene Datei komprimiert ist wie zum
            Beispiel Zip oder Arc. Diese Prüfung basiert auf der <code>MimeType</code> Prüfung und unterstützt
            die gleichen Methoden und Optionen. Diese Prüfung kann mit den dort beschriebenen Methoden auf
            spezielle Komprimierungstypen limitiert werden.
        </para>

        <example id="zend.file.transfer.validators.iscompressed.example">
            <title>Verwenden der IsCompressed Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Checkt ob die hochgeladene Datei komprimiert ist
$upload->addValidator('IsCompressed', false);

// Limitiert diese Prüfung auf Zip Dateien
$upload->addValidator('IsCompressed', false, array('application/zip'));

// Limitiert diese Prüfung auf Zip Dateien, verwendet aber eine einfachere Schreibweise
$upload->addValidator('IsCompressed', false, 'zip');
]]>
            </programlisting>
        </example>

        <note>
            <para>
                Es ist zu beachten das nicht geprüft wird ob ein gesetzter Dateityp ein Komprimierungstyp ist
                oder nicht. Man könnte also definieren das Gif Dateien von dieser Prüfung akzeptiert
                werden auch wenn das nicht logisch ist.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.isimage">
        <title><emphasis role="strong">IsImage</emphasis> Prüfung</title>

        <para>
            Die <code>IsImage</code> Prüfung checkt ob eine übertragene Datei eine Bilddatei ist wie zum
            Beispiel Gif oder Jpeg. Diese Prüfung basiert auf der <code>MimeType</code> Prüfung und unterstützt
            die gleichen Methoden und Optionen. Diese Prüfung kann mit den dort beschriebenen Methoden auf
            spezielle Bildarten limitiert werden.
        </para>

        <example id="zend.file.transfer.validators.isimage.example">
            <title>Verwenden der IsImage Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Checkt ob die hochgeladene Datei ein Bild ist
$upload->addValidator('IsImage', false);

// Limitiert diese Prüfung auf Gif Dateien
$upload->addValidator('IsImage', false, array('application/gif'));

// Limitiert diese Prüfung auf Jpeg Dateien, verwendet aber eine einfachere Schreibweise
$upload->addValidator('IsImage', false, 'jpeg');
]]>
            </programlisting>
        </example>

        <note>
            <para>
                Es ist zu beachten das nicht geprüft wird ob ein gesetzter Dateityp ein Bildtyp ist
                oder nicht. Man könnte also definieren das Zip Dateien von dieser Prüfung akzeptiert
                werden auch wenn das nicht logisch ist.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.hash">
        <title><emphasis role="strong">Hash</emphasis> Prüfung</title>

        <para>
            Die <code>Hash</code> Prüfung checkt den Inhalt einer übertragenen Datei indem Sie gehasht wird.
            Diese Prüfung verwendet die Hash Erweiterung von PHP. Sie unterstützt die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Setzt den Hash gegen den geprüft werden soll.
                </para>

                <para>
                    Man kann mehrere Hashes setzen indem Sie als Array angegeben werden. Jeder von Ihnen wird
                    geprüft und nur wenn alle fehlschlagen wird die Prüfung selbst fehlschlagen.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>algorithm</code>: Setzt den Algorithmus der für das Hashen des Inhalts verwendet
                    wird.
                </para>

                <para>
                    Man kann mehrere Algorithmen setzen indem die <code>addHash()</code> Methode mehrere
                    Male verwendet wird.
                </para>
            </listitem>
        </itemizedlist>

        <example id="zend.file.transfer.validators.hash.example">
            <title>Verwenden der Hash Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Prüft ob der Inhalt der hochgeladenen Datei den angegebenen Hash hat
$upload->addValidator('Hash', false, '3b3652f');

// Begrenzt diese Prüfung auf zwei unterschiedliche Hashes
$upload->addValidator('Hash', false, array('3b3652f', 'e612b69'));

// Setzt einen anderen Algorithmus gegen den geprüft wird
$upload->addValidator('Hash', false, array('315b3cd8273d44912a7', 'algorithm' => 'md5'));
]]>
            </programlisting>
        </example>

        <note>
            <para>
                Diese Prüfung unterstützt über 34 verschiedene Hash Algorithmen. Die bekanntesten sind
                'crc32', 'md5' und 'sha1'. Wenn man die anderen wissen will kann man in PHP's
                <ulink url="http://php.net/hash_algos">hash_algos Methode</ulink> nachsehen.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.md5">
        <title><emphasis role="strong">Md5</emphasis> Prüfung</title>

        <para>
            Die <code>Md5</code> Prüfung checkt den Inhalt einer übertragenen Datei durch hashen. Diese
            Prüfung verwendet die Hash Erweiterung von PHP mit dem Md5 Algorithmus. Sie unterstützt die
            folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Setzt den Hash gegen den geprüft wird.
                </para>

                <para>
                    Man kann mehrere Hashes setzen indem Sie als Array angegeben werden. Jeder von Ihnen wird
                    geprüft und nur wenn alle fehlschlagen wird die Prüfung selbst fehlschlagen.
                </para>
            </listitem>
        </itemizedlist>

        <example id="zend.file.transfer.validators.md5.example">
            <title>Verwenden der Md5 Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Prüft ob der Inhalt der hochgeladenen Datei den angegebenen Hash hat
$upload->addValidator('Md5', false, '3b3652f336522365223');

// Begrenzt diese Prüfung auf zwei unterschiedliche Hashes
$upload->addValidator('Md5', false, array('3b3652f336522365223', 'eb3365f3365ddc65365'));
]]>
            </programlisting>
        </example>
    </sect2>

    <sect2 id="zend.file.transfer.validators.mimetype">
        <title><emphasis role="strong">MimeType</emphasis> Prüfung</title>

        <para>
            Die <code>MimeType</code> Prüfung checkt den Mimetyp von übertragenen Dateien. Sie unterstützt
            die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Setzt die Art des Mimetyps der geprüft werden soll.
                </para>

                <para>
                    Mit dieser Option kann der Mimetyp von Dateien definiert werden der akzeptiert werden soll.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>magicfile</code>: Das Magicfile das verwendet werden soll.
                </para>

                <para>
                    Mit dieser Option kann das Magicfile definiert werden das zu verwenden ist. Wenn es nicht
                    gesetzt wird, oder leer ist, wird stattdessen die MAGIC Konstante verwendet. Diese
                    Option ist seit dem Zend Framework 1.7.1 vorhanden.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Diese Prüfung akzeptiert viele Mimetypen entweder als Komma-getrennter String, oder als
            Array. Man kan auch die Methoden <code>setMimeType()</code>, <code>addMimeType()</code>,
            und  <code>getMimeType()</code> verwenden um Mimetypen zu setzen und zu erhalten.
        </para>

        <para>
            Man kann mit der 'magicfile' Option auch die Magicdatei setzen die von Fileinfo verwendet
            werden soll. Zusätzlich gibt es die komfortablen <code>setMagicFile()</code> und
            <code>getMagicFile()</code> Methoden die das spätere Setzen und Empfangen des Magicfile
            Parameters erlauben. Diese Methoden sind seit dem Zend Framework 1.7.1 vorhanden.
        </para>

        <example id="zend.file.transfer.validators.mimetype.example">
            <title>Verwendung der MimeType Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Begrenzt den Mimetyp aller gegebenen Dateien auf Gif Bilder
$upload->addValidator('MimeType', 'image/gif');

// Begrenzt den Mimetyp alle gegebenen Dateien auf Gif und Jpeg Dateien
$upload->addValidator('MimeType', array('image/gif', 'image/jpeg');

// Begrenzt den Mimetyp aller Dateien auf die Gruppe image
$upload->addValidator('MimeType', 'image');

// Ein anderes magicfile verwenden
$upload->addValidator('MimeType', false, array('image', 'magicfile' => '/path/to/magicfile.mgx'));
]]>
            </programlisting>
        </example>

        <para>
            Das obige Beispiel zeigt das es auch möglich ist den akzeptierten Mimetyp auf eine Gruppe von
            Mimetypen zu begrenzen. Um alle Bilder zu erlauben kann einfach 'image' als Mimetyp verwendet
            werden. Das kann für alle Gruppen wie 'image', 'audio', 'video', 'test', und so weiter
            gemacht werden.
        </para>

        <note>
            <para>
                Es ist zu beachten das die Verwendung von Mimetyp-Gruppen alle Mitglieder dieser Gruppe
                akzeptiert, selbst wenn die Anwendung diese nicht unterstützt. Wenn man 'image' erlaubt,
                erhält man auch 'image/xpixmap' oder 'image/vasa' was problematisch sein könnte. Wenn man
                nicht sicher ist ob die eigene Anwendung alle Typen unterstützt ist es besser nur
                definierte Mimetypen zu erlauben statt der kompletten Gruppe.
            </para>
        </note>

        <note>
            <para>
                Diese Komponente verwendet die <code>fileinfo</code> Erweiterung wenn Sie vorhanden ist.
                Wenn nicht wird Sie die <code>mime_content_type</code> Funktion verwenden. Und wenn dieser
                Funktionsaufruf fehlschlägt wird der Mimetyp verwendet der von HTTP angegeben wurde.
            </para>

            <para>
                Man sollte sich vor möglichen Sicherheitsproblemen vorsehen wenn weder <code>fileinfo</code>
                noch <code>mime_content_type</code> vorhanden sind. Der von HTTP angegebene Mimetyp ist nicht
                sicher und kann einfach manipuliert werden.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.notexists">
        <title><emphasis role="strong">NotExists</emphasis> Prüfung</title>

        <para>
            Die <code>NotExists</code> Prüfung checkt ob Dateien die angegeben werden existieren. Sie unterstützt
            die folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Checkt ob die Datei im gegebenen Verzeichnis nicht existiert.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Diese Prüfung akzeptiert mehrere Verzeichnisse, entweder als Komma-getrennter String, oder als
            Array. Es können aber auch die Methoden <code>setDirectory()</code>, <code>addDirectory()</code>,
            und <code>getDirectory()</code> verwendet werden um die Verzeichnisse zu setzen und zu erhalten.
        </para>

        <example id="zend.file.transfer.validators.notexists.example">
            <title>Verwendung der NotExists Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Das temporäre Verzeichnis für die Prüfung hinzufügen
$upload->addValidator('NotExists', '\temp');

// Zwei Verzeichnisse durch Verwendung der Array Schreibweise hinzufügen
$upload->addValidator('NotExists',
                      array('\home\images',
                            '\home\uploads')
                     );
]]>
            </programlisting>

        </example>
        <note>
            <para>
                Beachte das diese Prüfung checkt ob die Datei in allen gesetzten Verzeichnissen nicht existiert.
                Die Prüfung schlägt fehl, wenn die Datei in irgendeinem der angegebenen Verzeichnisse
                existiert.
            </para>
        </note>
    </sect2>

    <sect2 id="zend.file.transfer.validators.sha1">
        <title><emphasis role="strong">Sha1</emphasis> Prüfung</title>

        <para>
            Die <code>Sha1</code> Prüfung checkt den Inhalt einer übertragenen Datei durch hashen. Diese
            Prüfung verwendet die Hash Erweiterung von PHP mit dem Sha1 Algorithmus. Sie unterstützt die
            folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>*</code>: Man kann einen beliebigen Schlüssel oder ein nummerisches Array
                    verwenden. Setzt den Hash gegen den geprüft wird.
                </para>

                <para>
                    Man kann mehrere Hashes setzen indem Sie als Array angegeben werden. Jeder von Ihnen wird
                    geprüft und nur wenn alle fehlschlagen wird die Prüfung selbst fehlschlagen.
                </para>
            </listitem>
        </itemizedlist>

        <example id="zend.file.transfer.validators.sha1.example">
            <title>Verwenden der Sha1 Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Prüft ob der Inhalt der hochgeladenen Datei den angegebenen Hash hat
$upload->addValidator('Sha1', false, '3b3652f336522365223');

// Begrenzt diese Prüfung auf zwei unterschiedliche Hashes
$upload->addValidator('Sha1', false, array('3b3652f336522365223', 'eb3365f3365ddc65365'));
]]>
            </programlisting>
        </example>
    </sect2>

    <sect2 id="zend.file.transfer.validators.size">
        <title><emphasis role="strong">Size</emphasis> Prüfung</title>

        <para>
            Die <code>Size</code> Prüfung checkt die Größe von einzelnen Dateien. Sie unterstützt die
            folgenden Optionen:
        </para>

        <itemizedlist>
            <listitem>
                <para>
                    <code>min</code>: Setzt die mindimale Dateigröße.
                </para>

                <para>
                    Mit dieser Option kann die mindeste Dateigröße für einzelne Dateien die übertragen
                    werden definieren.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>max</code>: Setzt die maximale Dateigröße.
                </para>

                <para>
                    Mit dieser Option kann die Dateigröße für einzelne Dateien die übertragen werden limitieren.
                </para>
            </listitem>

            <listitem>
                <para>
                    <code>bytestring</code>: Definiert ob ein Fehler mit einer Benutzerfreundlichen Nummer
                    zurückgegeben wird, oder mit der reinen Dateigröße.
                </para>

                <para>
                    Mit dieser Option kann definiert werden ob der Benutzer '10864' oder '10MB' erhält.
                    Der Standardwert ist true was '10MB' zurückgibt.
                </para>
            </listitem>
        </itemizedlist>

        <para>
            Wenn nur ein String angegeben wird, wird dieser als <code>max</code> verwendet. Aber man kann
            auch die Methoden <code>setMin()</code> und <code>setMax()</code> verwenden um beide Optionen
            im Nachhinein zu setzen, sowie <code>getMin()</code> und <code>getMax()</code> um die aktuell
            gesetzten Werte zu erhalten.
        </para>

        <para>
            Die Größe selbst wird auch in der SI Schreibweise akzeptiert wie Sie von den meisten
            Betriebsystemen angegeben wird. Statt 20000 Bytes kann man auch
            <emphasis role="strong">20kB</emphasis> angeben. Alle Einheiten werden mit dem Basiswert
            1024 konvertiert. Die folgenden Einheiten werden akzeptiert: <code>kB</code>, <code>MB</code>,
            <code>GB</code>, <code>TB</code>, <code>PB</code> und <code>EB</code>. Wie erwähnt muss man
            berücksichtigen das 1kB gleich 1024 Bytes entspricht.
        </para>

        <example id="zend.file.transfer.validators.size.example">
            <title>Verwendung der Size Prüfung</title>

            <programlisting role="php"><![CDATA[
$upload = new Zend_File_Transfer();

// Limitiert die Größe einer Datei auf 40000 Bytes
$upload->addValidator('Size', 40000);

// Limitiert die Größe der angegebenen Datei auf maximal 4MB und  minimal 10kB
// zusätzlich wird im Fall eines Fehlers die reine Zahl statt einer
// Benutzerfreundlichen zurückgegeben
$upload->addValidator('Size', array('min' => '10kB', 'max' => '4MB', 'bytestring' => false));
]]>
            </programlisting>
        </example>
    </sect2>
</sect1>
<!--
vim:se ts=4 sw=4 tw=80 et:
-->
